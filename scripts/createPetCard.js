import {
	createElement,
	createElementWithId,
	addNewElement,
	addNewImageElement,
	addNewButton,
	addNewElementWithAction,
} from "./elements-utils.js";

export const createPetCard = (id, petsArray) => {
  let card;


  return card; 
}